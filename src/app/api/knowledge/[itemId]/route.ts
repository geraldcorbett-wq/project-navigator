import { NextRequest,NextResponse } from "next/server";
import { authenticateRequest,isAuthFailure } from "../../../../lib/api/auth";
import { cleanOptionalText,jsonError,readJsonObject } from "../../../../lib/api/json";
export async function PATCH(request:NextRequest,{params}:{params:{itemId:string}}){const auth=await authenticateRequest(request);if(isAuthFailure(auth))return jsonError(auth.error,auth.status);const body=await readJsonObject(request);if(!body)return jsonError("A JSON object is required.",400);const patch:Record<string,unknown>={};for(const [k,n] of [["title",240],["content",50000],["url",2000],["kind",40]] as const)if(body[k]!==undefined)patch[k]=cleanOptionalText(body[k],n);if(body.tags!==undefined)patch.tags=Array.isArray(body.tags)?body.tags.slice(0,30):[];if(body.metadata!==undefined)patch.metadata=body.metadata;const {data,error}=await auth.supabase.from("knowledge_items").update(patch).eq("id",params.itemId).eq("user_id",auth.user.id).select("*").single();if(error)return jsonError(error.message,500);return NextResponse.json({item:data});}
export async function DELETE(request:NextRequest,{params}:{params:{itemId:string}}){const auth=await authenticateRequest(request);if(isAuthFailure(auth))return jsonError(auth.error,auth.status);const {error}=await auth.supabase.from("knowledge_items").delete().eq("id",params.itemId).eq("user_id",auth.user.id);if(error)return jsonError(error.message,500);return new NextResponse(null,{status:204});}
