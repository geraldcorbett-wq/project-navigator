import { NextRequest, NextResponse } from "next/server";
import { authenticateRequest, isAuthFailure } from "../../../lib/api/auth";
import { cleanOptionalText, jsonError, readJsonObject } from "../../../lib/api/json";
export const dynamic="force-dynamic";
export async function GET(request:NextRequest){const auth=await authenticateRequest(request);if(isAuthFailure(auth))return jsonError(auth.error,auth.status);const q=request.nextUrl.searchParams.get("q")?.trim();let query=auth.supabase.from("knowledge_items").select("*").eq("user_id",auth.user.id).order("updated_at",{ascending:false}).limit(100);if(q)query=query.or(`title.ilike.%${q.slice(0,200)}%,content.ilike.%${q.slice(0,200)}%`);const {data,error}=await query;if(error)return jsonError(error.message,500);return NextResponse.json({items:data});}
export async function POST(request:NextRequest){const auth=await authenticateRequest(request);if(isAuthFailure(auth))return jsonError(auth.error,auth.status);const body=await readJsonObject(request);if(!body)return jsonError("A JSON object is required.",400);const title=cleanOptionalText(body.title,240);if(!title)return jsonError("title is required.",400);const {data,error}=await auth.supabase.from("knowledge_items").insert({user_id:auth.user.id,kind:cleanOptionalText(body.kind,40)??"note",title,content:cleanOptionalText(body.content,50000)??null,url:cleanOptionalText(body.url,2000)??null,tags:Array.isArray(body.tags)?body.tags.slice(0,30):[],metadata:body.metadata??{}}).select("*").single();if(error)return jsonError(error.message,500);return NextResponse.json({item:data},{status:201});}
